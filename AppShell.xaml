<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="RecipeVault.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:RecipeVault"
    xmlns:page="clr-namespace:RecipeVault.Pages"
    Title="RecipeVault">

    <FlyoutItem Title="Main Page"
                Route="MainRoute">
        <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>

    <FlyoutItem Title="Recipe List"
                Route="ListRoute">
        <ShellContent
            ContentTemplate="{DataTemplate page:RecipeListPage}"
            Route="RecipeListPage" />
    </FlyoutItem>

</Shell>
